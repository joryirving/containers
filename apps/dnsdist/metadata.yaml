---
app: dnsdist
semver: true
channels:
  - name: stable
    platforms: ["linux/amd64", "linux/arm64"] # ["linux/amd64", "linux/arm64"]
    stable: true
    tests:
      enabled: false
      type: cli
