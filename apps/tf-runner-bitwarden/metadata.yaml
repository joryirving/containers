---
app: tf-runner-bitwarden
base: false
semantic_versioning: false
channels:
  - name: stable
    platforms: ["linux/amd64", "linux/arm64"] # ["linux/amd64", "linux/arm64"] arm64 is failing right now
    stable: true
    tests:
      enabled: false
