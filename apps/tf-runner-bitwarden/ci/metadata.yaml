---
app: tf-runner-bw
# not in use yet
url: "https://api.github.com/repos/weaveworks/tf-controller/tags"
rules:
  - "[0].name"
version_regex:
  - v*
channels:
  - name: stable
    platforms: ["linux/amd64"]
    stable: true
